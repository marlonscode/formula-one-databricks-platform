models:
  - name: fact_qualifying_result
    description: "Fact table for qualifying results"
    columns:
      - name: qualifying_key
        description: "Surrogate key for the qualifying result model"
        tests:
          - dbt_expectations.expect_column_to_exist
          - not_null
          - unique

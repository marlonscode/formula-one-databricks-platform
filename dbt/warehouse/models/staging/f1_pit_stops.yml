models:
  - name: f1_pit_stops
    description: "Staging table for F1 pit stops data"
    columns:
      - name: lap
        description: "The lap number during which the pit stop occurred"
        tests:
          - not_null
      - name: stop
        description: "The stop"
